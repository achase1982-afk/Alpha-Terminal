GEMINI_API_KEY=
# pragma: allowlist secret
# Desk TTS uses Gemini generateContent (same key). With AI_INTEGRATIONS_GEMINI_API_KEY, set AI_INTEGRATIONS_GEMINI_BASE_URL if your proxy hosts generateContent.
# Optional disk cache for desk TTS MP3 files (default /tmp/tts-cache)
# TTS_CACHE_DIR=
