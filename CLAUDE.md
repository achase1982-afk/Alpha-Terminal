# Alpha-Terminal

## Repository

GitHub: `achase1982-afk/Alpha-Terminal`
Feature branch: `claude/app-capabilities-d1kqms`
Base branch: `main`

## Required: Always Create a PR After Every Push

**After every `git push`, immediately create a pull request using `mcp__github__create_pull_request`.**

- Do not wait to be asked.
- Do not say "let me know if you want a PR."
- Create it automatically, every time, without exception.
- Include a clear title and summary of what changed.
- Base branch is always `main`.

## Git Workflow

1. Make changes on `claude/app-capabilities-d1kqms`
2. Commit with a descriptive message
3. `git push -u origin claude/app-capabilities-d1kqms`
4. Create PR immediately — no exceptions
