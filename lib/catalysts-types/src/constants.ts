/** Settled NYSE sessions used for Catalysts drift (sparkline, vs S&P, expanded rows). */
export const CATALYST_DRIFT_SESSION_COUNT = 10;

/** |stock − SPY| daily move above this (%) is treated as a bad bar at rebuild. */
export const CATALYST_SUSPECT_SESSION_REL_MOVE_ABS_PCT = 25;
