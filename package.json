{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.3",
  "license": "MIT",
  "pnpm": {
    "supportedArchitectures": {
      "os": ["current", "linux"],
      "cpu": ["current", "x64", "arm64"],
      "libc": ["current", "glibc", "musl"]
    }
  },
  "scripts": {
    "prepare": "command -v bash >/dev/null 2>&1 && bash scripts/install-git-hooks.sh || true",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --filter='!@workspace/mockup-sandbox' --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:migrate": "pnpm --filter @workspace/[REDACTED] run migrate:deploy"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
