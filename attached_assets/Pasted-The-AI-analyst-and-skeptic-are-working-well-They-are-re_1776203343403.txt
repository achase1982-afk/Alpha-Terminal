The AI analyst and skeptic are working well. They are reaching consensus on trades and saying PROCEED. Then the orchestrator code is rejecting every approved trade with three different code-level blocks. I need answers to these questions before we can fix it.

1. MAX_ACTIVE_REACHED: What is the current max active ideas limit? Where is it set in the code? Is it in the settings panel or hard-coded? MRVL was approved by both Claude and Gemini at least three separate times today and rejected every time by this limit. What counts as an “active” idea? Is it ideas that were approved and shown to the user? Or does it include rejected ideas? If the MU trade from earlier is the only PROCEED idea, why is MAX_ACTIVE being hit?
1. INVALID_EXPIRATION: CRDO and INTC were both approved by the analyst and skeptic, then rejected for invalid expiration. What expiration dates did Claude pick? What does the code consider a valid expiration? Is it checking against a list of actual expiration dates from the options chain? If Claude picks May 16, 2026 and that’s a real expiration Friday, why would it be invalid? Show me the exact validation logic.
1. LOW_OI: INTC was approved then rejected for low open interest. What is the minimum OI threshold? Is it in the settings panel or hard-coded? What strike and expiration did Claude pick, and what was the actual OI on that strike?
1. COOLDOWN LOGIC: When a trade is rejected by MAX_ACTIVE, INVALID_EXPIRATION, or LOW_OI, does the ticker go on any kind of cooldown? Or does it just get picked again on the next pass? Because MRVL, CRDO, and INTC are being picked and rejected repeatedly in a loop all day long. That is wasting API tokens on trades that will never get through. After a code-level rejection, the ticker should be excluded from the next 2-3 passes minimum.
1. ANALYST CONSTRAINTS: The analyst prompts currently say “You have NO knowledge outside the snapshot you receive. All analysis must come from these summaries.” I want that removed. The analyst should be able to use their training knowledge about the stock, the sector, recent news, market context – anything they know. The snapshot is additional data, not the only data. Same for the skeptic. Remove any language that restricts them to snapshot-only analysis.
1. Show me all the code-level validation checks that run AFTER the analyst and skeptic reach consensus. Every single check that can turn a PROCEED into a REJECT. I want the complete list so I can decide which ones to keep, which to loosen, and which to remove.