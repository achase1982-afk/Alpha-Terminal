Here’s the prompt:
Two fixes for the WebSocket order alert system. Fix 1: Replace the extractAcctFields() regex-based JSON field extraction in schwabStreamer.ts with a proper recursive object traversal function. The current approach regex-searches the serialized JSON string for Symbol, Instruction, Quantity, ExecutionPrice, and OrderType. This is brittle because JSON key ordering and formatting can change. Instead, write a small recursive function that takes the parsed JSON object and a list of target key names, walks the entire object tree (including nested objects and arrays), and returns the first match found for each key. Use JSON.parse() on the raw payload first, then pass the parsed object to this traversal function. Keep the same field names the rest of the code expects. Make sure it handles the case where a key is not found by returning undefined rather than throwing. Fix 2: Add deduplication to the order alert pipeline so that placing or canceling an order through Alpha Terminal does not produce duplicate push notifications. The problem is that the portfolio.ts route handlers broadcast an immediate orderAlert when an order is placed or canceled, and then the ACCT_ACTIVITY WebSocket handler broadcasts the same event again when Schwab confirms it. The fix: create a simple seen-set (Map or Set) in the schwabStreamer.ts module that stores a composite key of orderId plus event type. Before broadcasting any orderAlert from processAcctActivity(), check if that key exists in the seen-set. If it does, skip the broadcast. If it does not, add it and broadcast normally. Also, when the portfolio.ts route handlers broadcast their immediate alert, they should add the same composite key to the seen-set. Use a 60-second TTL on entries so the set does not grow unbounded. A simple approach is to store the timestamp with each entry and periodically prune entries older than 60 seconds, or use setTimeout to delete each entry after 60 seconds. Export the seen-set add function from the schwabStreamer module so portfolio.ts can import and use it. Do not change any existing push notification logic or alert types. Do not modify the silent vs push-worthy event classification. Just prevent the same orderId plus event type combination from being broadcast and push-notified twice.​​​​​​​​​​​​​​​​