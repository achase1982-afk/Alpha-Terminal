Here’s the corrected prompt:
Build the Deterministic Scanner Pipeline on the server side. This is a user-initiated scan that evaluates all symbols in the user’s watchlist for trade candidates using a hard scoring system. The scanner is NOT a background process. It only runs when the user taps a scan button on the Scanner tab. It does not push notifications or run on a schedule. The user decides when to look. The scanner has 4 stages that run in sequence. Stage 1 Universe Filter: Take all symbols in the user’s watchlist and eliminate any that fail basic criteria. Remove any ticker that has earnings within 5 trading days using the event calendar integration that was just built. Remove any ticker where the options chain has insufficient liquidity, defined as the at-the-money bid-ask spread being wider than 10 percent of the mid price. Remove any ticker that has a pending position already open in the portfolio to avoid doubling up. Check portfolio positions via the existing Schwab portfolio endpoint. Remove any ticker where data is stale or missing for the indicators needed in Stage 2. Log how many tickers passed and failed the filter and why. Stage 2 Technical Scoring: For each ticker that passed Stage 1, compute a technical score from 0 to 100 based on deterministic criteria. The score is the sum of weighted components. Component 1 Trend Alignment worth 25 points: Is the ticker’s price above or below its 20-day and 50-day moving averages? If both align in the same direction as the current Market Pulse bias, full points. If one aligns, half points. If neither aligns, zero. Component 2 Relative Strength worth 20 points: Compare the ticker’s percent change over 5 days to SPY’s percent change over 5 days. If the ticker is outperforming SPY in the direction of the Pulse bias, full points. Scale linearly based on the degree of outperformance. Component 3 Volume Confirmation worth 20 points: Is today’s volume above the 20-day average volume? Full points if volume is 1.5x or greater. Scale linearly between 1.0x and 1.5x. Zero below 1.0x. Component 4 Implied Volatility Rank worth 20 points: Score based on where current IV sits relative to its 52-week range. For directional trades, lower IVR is better since you are buying options. For premium selling, higher IVR is better. The scoring should flip based on the strategy type that would apply given the current Market Pulse mode. Component 5 Options Liquidity worth 15 points: Score based on average daily options volume for the ticker and the tightness of the at-the-money spread. Tighter spreads and higher volume get more points. Each component must be computed with pure math. No AI involvement in scoring. The code does the math. Stage 3 Ranking and Filtering: Sort all scored tickers by their total score descending. Apply a minimum score threshold of 60 to pass. If no tickers score above 60, return an empty result set with a message like “No high-conviction setups found. Cash is a position.” Do not lower the threshold or suggest marginal trades. Cap the output at the top 5 tickers maximum. Stage 4 Enrichment: For each ticker in the top 5, attach contextual data that the Strategist will need. Include the current Market Pulse composite score and confidence. Include the current Market Pulse bias direction. Include the ticker’s sector using GICS classification if available, or a simple sector label. Include upcoming events for that ticker from the calendar within the next 30 days. Include the current IVR and ATM implied volatility. Include the current bid-ask spread for ATM options. Include whether the ticker has weekly or only monthly options. Do not generate trade recommendations in the scanner. The scanner outputs scored candidates. The Strategist consumes them and decides what trade structure to recommend. For the frontend on the Scanner tab, display the results as a ranked list of cards. Each card shows the ticker, the total score out of 100, a mini breakdown of the 5 component scores as a horizontal bar or simple numbers, the sector, the key stat that drove the score like volume 2.1x average or IVR 65, and a button to send that ticker to the Strategist for a full recommendation. If a scan returns no results, display the “no setups found” message prominently. Do not suggest the user try again or lower their standards. Add a scan button at the top of the Scanner tab. When tapped, show a loading state while the 4 stages run. Display a brief summary of the filter stage like “X symbols scanned, Y passed filters, Z scored above threshold” where X is the actual watchlist count. Show a timestamp of when the scan was run. The scan results should persist on the tab until the user runs another scan. They should not auto-refresh or disappear. The scanner must respect the Regime Shock state. If shockActive is true, the scan button should be disabled and the tab should show “Scanning paused: regime shock active” instead of results. For Mode 3 Micro-Override qualification, a scan result needs a score of 90 or above, volume of 1.5x or greater, and relative strength confirmation. Tag any result that meets all three criteria with a “Micro-Override eligible” badge on the card. This tells the user this setup is strong enough to trade even when the Pulse shows no edge. The scanner also must check that a Micro-Override candidate is not trading inverse to a clear macro direction from the Pulse. If the Pulse composite is positive and the ticker setup is bearish, or vice versa, do not tag it as Micro-Override eligible. For data sources, use the Schwab API for price data, moving averages, volume, and options chain data. Compute the moving averages and relative strength from historical price data. Get IVR from the options chain implied volatility relative to 52-week high and low IV. Do not create any new WebSocket subscriptions. Use REST API calls since the scanner is on-demand not real-time. Do not add background scanning. Do not add scan scheduling. Do not send push notifications from the scanner. Do not add opportunity alerts. The scanner runs when the user asks it to run and shows results on screen. That is all.​​​​​​​​​​​​​​​​