Build a Trade Context Journal that captures information Schwab does not track. When the user places an order through Alpha Terminal’s Risk Gate, automatically save a journal entry to a Drizzle ORM table. Each entry contains: a unique id, the Schwab order ID returned from the order placement response, ticker symbol, strategy type and direction, the Market Pulse composite score and confidence and bias at the moment of entry, the scanner score if the trade came from the scanner or null if manual, which trading mode was active (1 through 4), the event calendar conflicts that were present at entry, the IVR at entry, the entry credit or debit, the max loss and max gain calculated by the Risk Gate, the thesis which is the Claude narrative summary from the Strategist trimmed to the first paragraph, and the createdAt timestamp. This is write-once at entry time. Do not poll Schwab to update these records. Do not track order state here. Do not duplicate anything Schwab already manages. For trades placed through TOS or externally, do not create a journal entry since Alpha Terminal has no context for those trades. When a position is closed, the user can optionally tap a “Log Result” button that lets them record: the exit price, the realized P&L, a short free-text note about what they learned, and whether the trade followed the plan or was a deviation. This is manual and optional. Do not auto-populate from Schwab. The user reflects and logs it themselves. Add a Journal tab accessible from the Portfolio section. It shows a list of journal entries sorted by date with the ticker, strategy, entry date, Pulse score at entry, mode, and P&L if logged. Tapping an entry shows the full detail including the Strategist narrative and the user’s notes. Add a simple stats summary at the top of the Journal tab: total trades logged, win rate based on entries that have results logged, average P&L, and a breakdown by mode showing how many trades were taken in each mode and the win rate per mode. This helps the user see if Mode 3 Micro-Overrides are actually profitable or if they should stop taking them. Do not build exit order management. Do not build position monitoring. Do not track order state. This is a journal only. That is all.