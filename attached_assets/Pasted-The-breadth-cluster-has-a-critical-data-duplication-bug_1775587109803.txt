The breadth cluster has a critical data duplication bug. During live market hours, ADVN = DECN = ADD = 1238.00, UVOL = DVOL = 787.9M. These are impossible values – advancing and declining issues can never be equal, up volume and down volume can never be equal. The data extraction is copying one field into multiple fields. Check how the Schwab WebSocket or IBKR Gateway maps breadth symbols into the cache. Each symbol needs its own distinct field mapping. Also add a sanity check: if ADVN = DECN exactly, or UVOL = DVOL exactly, force the entire breadth cluster to STALE.”
CBOE put/call ratios still missing (CPC, CPCE, CPCI). The IBKR Gateway fix from earlier isn’t working in this session. Only the Schwab-computed per-ticker ratios (PCSPY, PCQQQ, PCIWM) are flowing. Check if the IB Gateway connection dropped.
MOVE has no value. This is experimental so it’s fine, but SKEW at 0.00 is wrong during market hours. SKEW typically ranges 120-160. Add SKEW to the sanity check: if SKEW = 0.00 during RTH, force STALE.
/BZ (Brent) at 0.00 is clearly broken. /CL is at 112.87 so Brent should be around 109-110. The macro oil average calculation is using (112.87 + 0.00) / 2 = 56.43%, which would produce a completely wrong oil score. Add a guard: if /BZ = 0.00 and /CL > 50, skip /BZ and use /CL alone for the oil calculation.
/DX (Dollar Index) missing. Without this, the macro cluster is missing its dollar signal. The /6E euro is there (+0.28%) so the engine has partial currency data but can’t do the /6E vs /DX confirmation check.
What’s actually working correctly: Rates (all 9 fresh and realistic), Credit (all 3), Vol Level (7/8), Risk Appetite (all 7), and most of Macro. The composite of -0.40 and SLIGHTLY_BEARISH bias is probably directionally correct given futures are all red and VIX is up 9.68%, but the confidence of 34/100 is likely too high because the engine thinks breadth is providing real data when it’s garbage.
The breadth bug is the priority fix. It’s your highest-weighted cluster at 18% and right now it’s feeding the engine hallucinated data.​​​​​​​​​​​​​​​​