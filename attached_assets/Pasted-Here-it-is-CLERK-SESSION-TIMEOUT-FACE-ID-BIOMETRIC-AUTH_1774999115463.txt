Here it is:

CLERK SESSION TIMEOUT + FACE ID / BIOMETRIC AUTH. This is a two-part implementation. Do not touch anything outside of what is described here. Do not touch Schwab OAuth, the scoring engine, the AI streaming routes, or any market data logic.
PART 1: Session Timeout – Dynamic, User-Configurable. The session timeout must be fully dynamic. The value is never hardcoded anywhere in the codebase. Not in the Clerk dashboard, not in any config file, not in any component. Every timeout duration must be driven entirely by the user’s selected preference stored in alphaTerminalSecurityPrefs.sessionTimeout. The available options are: 15 minutes, 30 minutes, 60 minutes, 90 minutes, and Never. The default is 30 minutes. In code, track the last user interaction timestamp using document.addEventListener on mousemove, keydown, touchstart, and scroll. Store the timestamp in memory. Run a check every 60 seconds. On each check, read the current value of alphaTerminalSecurityPrefs.sessionTimeout from localStorage, calculate the threshold in milliseconds, and compare against last activity time. If the threshold is exceeded and the option is not Never, call clerk.signOut(). This means if the user changes their timeout preference from 30 minutes to 90 minutes in Settings, the very next 60-second check will use 90 minutes. No reload required. No hardcoded values anywhere. The logic reads the preference fresh on every single check so any change takes effect immediately. If the user selects Never, the inactivity check still runs but clerk.signOut() is never called. Test every single option – 15 min, 30 min, 60 min, 90 min, and Never – and confirm each one behaves correctly before considering this done.
PART 2: Face ID / Biometric Authentication. Use the Web Authentication API (WebAuthn) which is natively supported in Safari on iOS and Chrome on Android. Do not use any third-party biometric library. Clerk supports passkeys natively – use clerk.user.createPasskey() for registration and Clerk’s passkey sign-in flow for authentication. Implement biometrics in three places. First, App Login: add a Sign in with Face ID button on the login screen. If the user has a registered passkey, attempt biometric authentication first before showing the email/password form. If biometrics fail or are unavailable, fall back to the standard Clerk login form. Second, Sensitive Data Re-auth: when the user navigates to the AI tab or any screen showing market pulse data, check if more than 5 minutes have passed since their last biometric confirmation. If yes, show a biometric prompt before revealing the content. If the user declines, show the content anyway but log the bypass. Third, Pre-Trade Re-auth: before the Options Strategist generates a strategy recommendation, require biometric confirmation. Show a modal that says Confirm with Face ID to continue. If biometrics are unavailable on the device, skip this step silently. All three biometric gates must respect the user’s Security Settings preferences described in Part 3. If a gate is toggled off in settings, skip it entirely. Never show a biometric prompt for a gate the user has disabled.
PART 3: Security Settings UI. Under the existing Settings section in the sidebar, add a Security subsection. Store all preferences in localStorage under the key alphaTerminalSecurityPrefs using this structure: sessionTimeout as a number in minutes, biometricLogin as a boolean, biometricSensitiveData as a boolean, and biometricTradeConfirmation as a boolean. Build the following controls. Session Timeout: a segmented control with five options: 15 min, 30 min, 60 min, 90 min, and Never. Whatever the user selects takes effect immediately on the next 60-second inactivity check with no reload required. Biometric Login toggle on/off: controls whether Face ID is used at app login. When off, only the standard Clerk email/password form is shown. Biometric for Sensitive Data toggle on/off: controls the re-auth gate on the AI tab and market pulse screen. When off, those screens load without any biometric prompt. Biometric for Trade Confirmation toggle on/off: controls the biometric prompt before the Options Strategist generates a recommendation. When off, the strategist runs immediately without any confirmation step. All settings must apply immediately without a page reload. If the device does not support WebAuthn, hide all three biometric toggles and display a single note beneath them that reads: Biometric authentication is not supported on this device. The session timeout control must always be visible regardless of device capability.
CRITICAL RULES: No hardcoded timeout values anywhere in the codebase. No hardcoded biometric behavior anywhere in the codebase. Every behavior is driven by the values in alphaTerminalSecurityPrefs. Do not touch Schwab OAuth, the scoring engine, AI streaming routes, or any market data logic. Do not rebuild any existing components. Test every timeout option and every biometric toggle before considering this complete.​​​​​​​​​​​​​​​​