Role: Senior Python UI/UX Developer. Objective: Refine the AlphaTerm dashboard based on user testing. We need to fix symbol formatting for the Schwab API, improve main-screen navigation, make the macro cards dynamic, and restore an interactive scrolling ticker. Task 1: Schwab Symbology & Quote Fixing. Create a symbol formatting function before making Schwab API quote requests. If the user inputs common indices (SPX, VIX, NDX, RUT), append the correct prefix/suffix required by Schwab's quote endpoint (e.g., format as $VIX.X or $SPX.X as needed by the API). If the user inputs a future (e.g., starting with /, like /ES), ensure the code gracefully uses the correct Futures Quote endpoint via the Schwab API library, or fails gracefully by just displaying the chart without hanging on "Awaiting Market Data." Task 2: Main Screen Search & Navigation. Remove the "Active Ticker" search input from the expandable sidebar. Place the Ticker Search Bar prominently on the main screen, directly below the top Macro Cards and above the main Chart/Options/AI tabs. It must be instantly accessible without opening any menus. Task 3: Dynamic Macro Cards (User Settings). Currently, the 4 Macro Cards (SPY, QQQ, IWM, VIX) are hardcoded. Add a "Dashboard Settings" section in the sidebar. Include text inputs allowing the user to type in 4 custom tickers for their Macro Cards. Store these in the session state so the user can change them on the fly without editing the code. Update the Macro Cards to reflect these variables. Task 4: The Interactive Scrolling Marquee. Restore a scrolling ticker strip at the very top of the app. Because standard HTML <marquee> doesn't trigger Python callbacks easily, use streamlit-components (or standard Dash callbacks) with custom HTML/JS to create a horizontal auto-scrolling row of popular tickers. Crucial: These scrolling tickers MUST be clickable. Clicking one must update the active_ticker state and reload the main dashboard for that symbol. Please review main.py and implement these structural fixes immediately. Ensure robust error handling if a symbol's quote cannot be fetched so the app does not hang.
