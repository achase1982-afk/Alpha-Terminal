The Ticker Header needs an immediate redesign to meet professional trading-terminal standards (like Thinkorswim or Bloomberg). The current design fails visual hierarchy and WCAG accessibility standards.

Please update the JSX and Tailwind CSS for the Ticker Header exactly as follows:

1. LEFT ZONE: Ticker Identity
- Ticker Symbol (e.g., "TSLA"): Must be large and bold (e.g., `text-xl font-bold text-white`).
- Company Name: Must feel like a footnote, not a co-headline. Use `text-xs font-normal text-zinc-500 tracking-wider uppercase truncate`.

2. CENTER ZONE: Last Price (The Primary Visual Anchor)
- This must be the single most dominant element on the screen.
- "LAST PRICE" Label: Must be extremely muted so it doesn't compete. Use `text-[10px] uppercase tracking-[0.1em] text-zinc-500 font-semibold`.
- Last Price Number ($360.41): Use `text-4xl lg:text-5xl font-extrabold text-white tracking-tighter`. 
- CRITICAL: Apply `font-variant-numeric: tabular-nums;` (or Tailwind `tabular-nums`) to all prices so the digits align perfectly and don't jitter when the price changes.
- Daily Change (-$11.70): Keep the red/green semantic coloring, but use `text-sm font-medium`. Ensure it stays on one line.

3. RIGHT ZONE: SELL / BUY Action Buttons (Institutional Style)
These must look like highly clickable, premium order triggers, NOT floating text.
- Create two equal-sized, fixed-width buttons (e.g., `min-w-[90px] min-h-[48px]`).
- SELL Button Container: Use a solid tinted fill with a bright border. `bg-red-950/40 border border-red-500/50 rounded-lg flex flex-col items-center justify-center p-2 cursor-pointer transition-colors hover:bg-red-900/60 active:bg-red-800/70`.
- BUY Button Container: `bg-emerald-950/40 border border-emerald-500/50 rounded-lg flex flex-col items-center justify-center p-2 cursor-pointer transition-colors hover:bg-emerald-900/60 active:bg-emerald-800/70`.
- Button Labels ("SELL" / "BUY"): Must be small but bright. Use `text-[10px] uppercase font-bold tracking-widest` with `text-red-400` for Sell and `text-emerald-400` for Buy.
- Button Prices (The Bid/Ask): This is non-negotiable for legibility—the numbers MUST be pure white. Use `text-lg font-bold text-white tabular-nums`. 

4. LAYOUT & SPACING
- Ensure the header uses a Flexbox or Grid layout with `items-center` and `justify-between`. 
- Add a gap between the SELL and BUY buttons (e.g., `gap-2`).
- Add a clear divider or breathing room between the Last Price zone and the Action Buttons zone.
