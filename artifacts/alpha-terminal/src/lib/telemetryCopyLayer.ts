/**
 * Stacking index for Telemetry “copy sections” UI (popover / sheet).
 * Must clear the Settings Telemetry portal (`z-[100]`), terminal header (`z-[105]`),
 * AI sticky chrome (`z-40`), and Search overlay (`z-[900]`). Stay below Watchlist editor (`z-[10000]`).
 */
export const TELEMETRY_COPY_LAYER_Z = 10050;
